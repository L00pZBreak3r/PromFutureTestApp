using PromFutureTestApp.Constants;

namespace PromFutureTestApp.Components.Layout;

public partial class MainLayout
{
    private const string Title = AppConstants.MainTitle + " - " + AppConstants.Title;
    private const string UserTitle = AppConstants.UserTitle;
}
